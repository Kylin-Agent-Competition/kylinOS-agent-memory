# PR #42 第二轮 REWORK L0 静态/编译检查 — 麒麟 VM 目标宿主复验

- **验证项**：`deliverables/PR42_REWORK_ACTION_ITEMS_20260818.md` §一「代码 L0 修复」1.1~1.5 的目标宿主复验
- **证据性质**：L0 静态检查 / 编译（**非 L2 Runtime 证据**，依 AGENTS.md 测试分层：静态检查 ≠ Runtime Test）
- **复验日期**：2026-08-19
- **验证人**：周子腾（D）
- **执行环境**：麒麟 VM（KylinOS V11 x86_64），SSH 127.0.0.1:2222
- **源日志**：同目录 `pr42_l0_verify_20260819.log`
- **对应行动项**：清单 §1.1（deploy_hook.sh 语法）、§1.2/§1.5（C 静态编译 + gcc 12.3.0 复验）、§1.3（test_connect_hook.sh 语法）

---

## 一、环境基线

| 项 | 值 |
|----|----|
| 内核 | Linux 6.6.0-76-generic x86_64 |
| 发行版 | 银河麒麟桌面 OS V11 |
| gcc | 12.3.0（openKylin 12.3.0-1ok3k0.1） |
| bash | 5.2.21(1)-release |

## 二、传输完整性（SHA256 3/3 MATCH）

| 文件 | SHA256 |
|------|--------|
| libconnect_hook.c | c6031528fc52b670affab922ab6872f5a8bf52831aeb159b99c1bb5fbecdbe8f |
| deploy_hook.sh | a188817ead69b6da3c1ea14600ba7a547ae0a59a0cbd0f173fed4f625f234737 |
| test_connect_hook.sh | 41310f6cca998c49499284e2baa3d8e26de9cea0d6d460c603d1ac47e778b900 |

> `libconnect_hook.c` 的 SHA256（`c6031528…`）与 `PR42_REWORK_ACTION_ITEMS_20260818.md` §1.5 记载一致。

## 三、验证结果

| 行动项 | 命令 | 结果 |
|--------|------|------|
| 1.1 deploy_hook.sh 语法 | `bash -n` | PASS（exit=0） |
| 1.3 test_connect_hook.sh 语法 | `bash -n` | PASS（exit=0） |
| 1.2 / 1.5 C 静态编译 | `gcc -std=c11 -Wall -Wextra -fsyntax-only` | PASS（exit=0，无 warning/error） |
| 完整 .so 编译 | `gcc -shared -fPIC -O2 -pthread -ldl -Wall -Wextra` | PASS（exit=0），导出 `T connect`，链接 `pthread_once@GLIBC_2.34` |

## 四、诚实边界

- 本证据为 **L0 静态检查 / 编译**在麒麟 VM 目标宿主（gcc 12.3.0）上的复验，**不作为 L2/L3 Runtime 证据**。
- `bash -n` 仅验证 Shell 语法，不执行脚本；`gcc -fsyntax-only` 仅验证编译，不运行产物。
- 完整 `gcc -shared` 编译通过并导出 `connect` 符号（`nm -D` 显示 `T connect`），仅证明「目标宿主工具链可构建出可被 LD_PRELOAD interpose 的 .so」，**不等于**端到端 Hook 运行时验证（后者见 `D4-OPENKYLIN-HOOK`，仍为 PARTIAL）。
- 本复验不覆盖第三轮（`PR42_R3_REWORK_ACTION_ITEMS_20260819.md`）新增的 HIGH-02 CMake `-fvisibility=hidden` 导出验证（手工 gcc 路径 ≠ CMake 路径）。

## 五、签署

| 角色 | 姓名 | 日期 | 结论 |
|------|------|------|------|
| 验证人 | 周子腾（D） | 2026-08-19 | L0 静态/编译检查 4/4 PASS（麒麟 VM gcc 12.3.0 复验） |
| Reviewer | 谢嘉然（E） | 2026-08-20 | 已签署 |
