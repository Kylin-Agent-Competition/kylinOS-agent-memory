# D11D 麒麟 VM L2 证据：复审返工——权限 0700 对齐 + 回退恢复 + 完整生命周期转录（2026-09-02）

> 对应 Reviewer D 复审 MEDIUM-1/2/3（E 对齐）。被测生产代码 `origin/main@47af2fa`；VM `Kylin-V11-2603-D11D-47af2fa-Test`。

## 被测脚本（sha256 绑定）
- `packaging/systemd/install_kylin_memory.sh`：sha256 `0781900881eacc91024148c6759ff16ccc31fc73aabc5b6a944c2f8e1da7655f`
- `packaging/systemd/kylin-memory.service`：sha256 `cf8f76aab5da36dd5eaff6276a71f8307bf4511b1da135d2226af0402e120c28`
- 即本 PR 返工提交后的最终脚本版本（转录日志头部记录，可审计绑定）。

## L2 执行与结果（完整 command/exit code/stdout/stderr，tee 归档）

| 步骤 | 结果 |
|---|---|
| STEP1 install（升级，已有安装→新装） | exit=0；既有 unit/wrapper 均备份（`.bak.20260902_1151xx`）；active；重启复验 active |
| STEP2 restart | exit=0；active；socket OK；journal「Memory Service 就绪」 |
| STEP3 rollback（恢复安装前状态） | exit=0；**恢复原 unit + 原 wrapper**；停止+禁用；`inactive` 确认；文件恢复存在 |
| STEP4 re-install（最终态） | exit=0；active；socket/DB 0600 |

## 权限（冻结契约 §1.1 对齐，MEDIUM-1）

| 路径 | 权限 | 契约 |
|---|---|---|
| `~/.config/kylin-memory/` | 700 | 0700 ✅ |
| `~/.local/share/kylin-memory/` | 700 | 0700 ✅ |
| `~/.local/state/kylin-memory/` | 700 | 0700 ✅ |
| `$XDG_RUNTIME_DIR/kylin-memory/`（/run/user/1000/kylin-memory） | 700 | 0700 ✅（`RuntimeDirectoryMode=0700`） |
| memory.sock | 600 | 0600 ✅ |
| kylin_memory.db | 600 | 0600 ✅ |
| wrapper `~/.local/bin/kylin-memory-server` | 755 | 确定性 0755 ✅ |

## 回退语义（MEDIUM-2）

- install 覆盖前备份既有 unit 与 wrapper；rollback 恢复备份（`已恢复原 unit / 已恢复原 wrapper`），无备份时才删除。
- 本 L2 在“已有安装”前置上验证：升级 → rollback → 恢复原状（而非仅 clean→install→uninstall），符合“可回退到安装前状态”。

## 转录可审计性（MEDIUM-3②）

- 完整日志 `evidence/l2-kylin-vm/d11d_vm_rework_l2_20260902.log`：含脚本/unit sha256、pre-state、每步命令与 exit code、stdout/stderr、最终 journal；SHA-256 `832ad69ab6fc80a48318a01f90aff7dfc5f41126c998030738f0473764d769b6`（LF 归一化）。
- 已知提示：STEP4 重启复验时脚本日志 grep 偶发 WARN（时序），最终 journal 明确出现「Memory Service 就绪」（见转录日志尾部），不构成失败。

## 限制 / UNVERIFIED

- 正式发行环境（生产 Kylin）systemd 未验证；KYSEC 生产规则本 VM 不可见；检索/删除端到端跨轨 pending；Kaiming 包安装未实现。
- A+C 轨输入归档：`evidence/l2-kylin-vm/d11d_ac_track_input_20260902.md`（A 轨 47af2fa 实测、C 轨 1cd6df9 L0/静态证据；D11D 汇合点 VM 交互复测以 main 最新为准）。