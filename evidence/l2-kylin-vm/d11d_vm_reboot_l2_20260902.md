# D11D 麒麟 VM L2 证据：OS 整机重启与服务自启（第 7 项）＋ 同 Commit 全模块启动（第 8 项）

> 被测生产代码 `origin/main@47af2fa`；VM `Kylin-V11-2603-D11D-47af2fa-Test`（2026-09-02）。

## 第 7 项 OS 整机重启（PASS）

- 执行 `sudo reboot`（来宾侧）；SSH 断开后轮询恢复（约 2 分钟内）。
- 重启后 `uptime` 归零（up 1 min）；用户会话 `loginctl`：tty7/seat0 `active`。
- **`kylin-memory.service` 自动启动**：`active (running)` since 00:24:18，Main PID 1925（新进程），`enabled` 保持。
- socket `/run/user/1000/kylin-memory/memory.sock` `srw-------`（0600）存在。
- journal 确认重启前 Stopping（00:23:17）→ 重启后自动 Started。
- UDS health：`{"status":"ok","db":"ok","outbox":{"backlog":0,...}}`。
- 防睡眠配置持久：screensaver `lock-enabled=false`、`idle-delay=0`。
- 部署/回退可重复性见第 3 项证据（install/rollback/重装 exit=0）。

## 第 8 项 同 Commit 同 VM 全模块启动可追踪（D 侧 PASS；A/B/C 端到端输入跨轨 pending）

同一提交（47af2fa）同一 VM 上模块启动清单：

| 模块 | 状态 | 证据 |
|---|---|---|
| memory-service（kylin-memory.service，含 Outbox Worker） | active (running)，journal 可追踪 | 本证据 + 第 3/4 项 |
| 麒麟 AI Runtime（core-textembedding 等 UDS） | `/tmp/.kylin-ai-runtime-unix/1000/core-textembedding.sock` 存在 | 本证据 |
| vector-engine（kylin-ai-vector-engine.service） | active；`/tmp/kylin-ai-vector-engine-1000.sock` 存在 | 本证据 |
| 诊断链路（health/echo/retrieve + JSON 日志 trace_id） | ok | 第 4 项 |

跨轨 pending：`memory.retrieve` 返回真实空上下文（`retrieval main chain pending`）——B 轨 D11B（PR #111）与 C 轨 D11C 端到端输入未合入/未提供；不以 D 侧启动证明替代检索端到端。

## 限制 / UNVERIFIED

- 正式发行环境（生产 Kylin）systemd 行为未验证（本链接克隆）。
- KYSEC 生产规则本 VM 不可见，保持 `UNVERIFIED`。
- 检索/删除/遗忘端到端依赖 B/C/D10D 输入，跨轨 pending。
- 原始日志：`evidence/l2-kylin-vm/d11d_vm_reboot_l2_20260902.log`（SHA-256 `50431c4c0f87a2c4277e2ad62970bea3e0f5ddca8abd8ba16cb530e91f888e45`，LF 归一化）。