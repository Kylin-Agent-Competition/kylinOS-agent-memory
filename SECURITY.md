# 安全策略

## 禁止提交的内容

以下内容**严禁**出现在仓库中：

- API Key、Token、密码、私钥、证书（`.pem`、`.key`、`.crt` 等）
- 未脱敏的聊天记录
- 包含真实用户数据的数据库（`.db`、`.sqlite`、`.sqlite3`）
- 未脱敏的截图
- 系统动态库（`.so`、`.dll`、`.dylib`）
- 虚拟机镜像（`.iso`、`.vdi`、`.vmdk`、`.ova`、`.ovf`）
- 预训练模型文件（`.onnx`、`.engine` 等）
- `.env` 文件（除 `config/environment.example` 外）

## 漏洞报告

安全问题不得在公开 Issue 中披露细节。

请通过指定的安全渠道向项目团队私下报告。

## 严重等级定义

| 等级 | 示例 |
|------|------|
| **Critical** | 跨用户数据泄露、密钥写入日志、生产环境误删除、以 Mock 冒充 Runtime Test 并影响生产安全 |
| **High** | 未授权数据访问、IPC 配置缺陷导致任意用户连接 |
| **Medium** | 日志输出敏感信息（需本地访问）、配置错误导致功能降级 |
| **Low** | 信息泄露（需物理访问）、非关键路径竞态 |

## 密钥泄露处理

一旦发现密钥进入 Git 历史：

1. **立即轮换**受影响的密钥。
2. 清理 Git 历史中的敏感数据。
3. 通知所有相关方。
4. 记录事件原因与预防措施。

## 依赖安全

- 依赖更新前必须审查变更日志与安全通告。
- CI 中的依赖审计结果必须通过方可合并。
