# D15D 任务卡：发布候选与提交材料锁定（已签署并合并）

> 状态：`SIGNED_PREPARED / MERGED / LOCKED`
> G_D7：`G_D7_SIGNED`
> 边界：`L3_READY=true / release_ready=false / production_ready=false`
> 历史释放分类：`NEW_RELEASE_IDENTITY_BUILT_AND_VM_VERIFIED`
> （2026-09-10 Phase 0-4 材料已在 `docs/day15-d15d-lock` 分支核验。D14A contract
> 的 D14D `ba3b50e` 身份保留为 historical scope；D15D 当前 release identity 为
> `4a6323f`，由 D15D manifest v2 与 `20260910T205300Z` evidence root 承载。
> Reviewer E 已在 PR comment 5616426125 独立裁定 identity/provenance closure
> `APPROVED`；D15D/G-D7 最终签署由 review `5168962906` 完成）。
> 日期：2026-09-06
> 台账位置：`麒麟OS_Agent记忆系统_15天75项个人施工台账_修正版.xlsx` 第 80 行（D15 / D 轨）。
> 当前资产真源：`kylin-mem/main = ad782f5be747d9d59f273c1c0813535d62b50dcb`（下称
> `main@ad782f5`）。`release_commit` 仍是 `4a6323f`；`main@4a6323f` 到
> `main@ad782f5` 的 raw Runbook C2 locked-path diff 命中 PR #175 已审查的两个
> docs-only exception：D14A contract 与 `packaging/systemd/README.md`；另新增
> 两个 day15 文档。
> `main@cdcce34` 与 `main@306c15e` 均只保留为历史快照。
> 评审标准依据：`.pr-review/REVIEWER_E_REVIEW_STANDARDS.md`（Reviewer E，非作者 Reviewer）。

## 1. 任务与目标

D15D 的目标是锁定“可提交、可安装、可恢复”的最终虚拟机成品所需发布材料，使提交包内容、版本清单、
脚本/unit 哈希与仓库 main 一一对应，并取得 Reviewer E 的非作者签署。

本卡只负责**锁定与一致性核验**，不负责新的生产实现，不重跑 D14D 正式 L3。
D15D 在正式 run 前的状态只能是 `PREPARED_DRAFT / PENDING_E_SIGN`；未满足第 4 节 Gate 前
不得声明 `READY / LOCKED / 已签署`。

## 2. 范围

### 2.1 锁定范围（approved scope）

| 锁定对象 | main@3138e94 资产 | 锁定口径 |
| --- | --- | --- |
| 发布目录 | `packaging/release/` | 唯一正式发布入口；冻结 build/smoke/install/uninstall/verify 脚本内容 |
| systemd unit | `packaging/systemd/kylin-memory.service` | 包内 `systemd/kylin-memory.service` 与 source blob 一致；install 后 ExecStart 渲染为 `<install_prefix>/bin/kylin-memory-server` |
| 安装 | `packaging/release/systemd_install.sh` | 包内 `systemd/install.sh`；fail-closed 完整性 Gate + SDK identity + 迁移 + user unit/socket |
| 升级 | 无独立 `upgrade.sh` | 官方升级语义 = 事务化 `install.sh install`（捕获旧 prefix/unit/launcher）→ `uninstall.sh rollback`；验收入口 = `package_smoke.sh --scenario upgrade-rollback`（`PENDING_D/E_SIGN`） |
| 卸载/回退 | `packaging/release/systemd_uninstall.sh` | 包内 `systemd/uninstall.sh`；事务元数据驱动精确恢复，clean-state removal 仅在无事务时使用 |
| 验证 | `packaging/release/systemd_verify.sh` | 包内 `systemd/verify.sh`；真实独立 embedding server PID、SDK maps/SHA、dim=768 |
| 版本清单 | `D15D_VERSION_MANIFEST.json`（后续入库 `docs/day15/`） | 含 release_commit、tar/manifest/SHA256SUMS、脚本/unit SHA、VERSION_MAP 版本、D14D evidence 引用 |
| 提交包 | 固定内容清单 | 发布包 tar + 解包目录 + 版本清单 + 契约 + 证据索引；与 main 文件集一一对应、无多余文件 |
| 签署 Gate | 本卡 + 台账 +（后续）D15D PR | Reviewer E 正式 APPROVE；D 轨不自签、不代签 |

### 2.2 禁止改动（out-of-scope / prohibited）

- 冻结 IPC、Schema、数据库字段、错误码；
- D13E Dataset / Gold / Threshold / Runner 契约；
- `docs/day14/00_d14a_release_package_contract.md` v5 当前身份（当前 identity 已由
  三方守卫绑定 D14D `ba3b50e` evidence，且 Reviewer E 已专项裁定 identity closure
  `APPROVED`；最终 E 签署前不代签）；
- 官方 AI Assistant；
- `packaging/kaiming/` 生产实现（保持 `OUT_OF_SCOPE / DEFERRED`，`PENDING_D/E_SIGN`）；
- `packaging/systemd/` 历史脚本内容（历史/只读；如未来入 PR 只允许改 README 指向性说明，不改脚本）；
- 本阶段任何生产代码/测试变更；
- 把 WSL、Mock、静态检查或历史 D14A/D14D 日志冒充最终 Release Commit 的正式 L3 证据。

## 3. 角色与责任

| 角色 | 责任 |
| --- | --- |
| D15D 执行方（本工作区） | 起草任务卡、盘点、runbook；维护本地台账；催办 #160；不代签 |
| Reviewer E（非作者 Reviewer） | 独立审阅 D15D 材料/未来 D15D PR；批准 = 签署 Gate |
| D 主审（如需要） | 书面裁决升级口径、Kaiming/upgrade/旧 systemd 处理等 `PENDING_D/E` 项；不替代 E 签署 |
| D13D/D14D owner | #160 merge、最终 tested_commit、D14D 正式 run 材料 |

## 4. Gate 与验收（正式锁定前置）

| Gate | 通过条件 | 失败处置 |
| --- | --- | --- |
| G-D0 基线 | 记录唯一 `release_commit`（40 位）；工作树干净；与 main 祖先关系明确 | 停；重选/重建后重签 |
| G-D1 包锁定 | 消费已由 D14D G2 验证的原始 frozen tar：tar/manifest/SHA256SUMS/VERSION 唯一并记录；仅当 tar/manifest/脚本 identity drift/mismatch 时才从 `release_commit` 重建并重跑一致性链 | 停；不改旧包；mismatch 不得沿用 |
| G-D2 内容一致 | 包内 `systemd/install.sh|uninstall.sh|verify.sh|kylin-memory.service` 与 `main@release_commit` 对应 blob 哈希一致 | 停；重建包 |
| G-D3 测试基线 | CI `d14a-packaging-provenance` 与仓库基线验证通过；相关 L0/L1 可复现 | 停；不锁定失败基线 |
| G-D4 D14D 消费 / release binding | 满足其一：(1) 当前 release 的 D14D 正式 G0–G9 root 与 release_commit/tar/manifest 全量绑定；(2) 正式采用 D15D clean-tar VM evidence root 承载 release binding，且该 root 含 summary、gate matrix、evidence index/checksums 与 failed→final run 选择关系。两种路径都不得把历史 D14D `ba3b50e` root 冒充当前 release 的 G0–G9；runtime/model G0 身份仍作为参考输入 | `BLOCKED`，不进入最终锁定 |
| G-D5 版本清单 | `D15D_VERSION_MANIFEST.json` 中全部值与包 manifest、VERSION_MAP、D14D evidence 三方一致 | 停；修复清单 |
| G-D6 提交包 | 当前 `4a6323f-r2` 有 submission inventory，绑定 tar、解包目录/manifest、D15D manifest v2、contract 与 evidence index；包 manifest ↔ tar 条目比较和全量 checksums 闭合 | 停；修正提交包 |
| G-D7 签署 | Reviewer E 对 D15D PR/材料 APPROVE；任务卡升 `SIGNED_PREPARED`；台账“已审查→已合并”按序推进 | 不自行标已审查/已合并 |

## 5. 当前输入与状态

| 输入 | 当前状态 | 说明 |
| --- | --- | --- |
| 资产真源 `main@4a6323f` | AVAILABLE | 2026-09-10 第 4 轮返工后刷新；PR #156 已合入 |
| `release_commit` | `4a6323fb3a8c73e0b15f1f3629d28dfc12071541` | current main；old `ba3b50e` lock 仅保留为 `FAIL_INVALIDATED` historical record |
| package tar/manifest/SHA256SUMS | AVAILABLE（`4a6323f-r2` rebuild） | tar `974c2584a08bc2ae5277a8526ce3c5dbd711bc0d54c9844e99d658892cca9f28`；manifest `4b42d9281e1fac269bc0e0ba43d831317424fc6200a751380ebf985241124ec4`；SHA256SUMS `9d1ac01fab8a2a876b97ffc5ffd375085661c78029b03a35c6984b7c91f4d9f4` |
| D13D #160 | MERGED | `ba3b50e1bdeea185bca9daee9d1d45958f62a636` |
| D14D FORMAL L3 G0–G9 | G0–G6 PASS；G7=N-A；G8=NOT_RUN waiver；G9 evidence | Evidence root：`evidence/l3-kylin-vm/d14d_20260907T141000Z_ba3b50e` |
| runtime/model 身份 | EXTRACTED_FROM_D14D_G0 / C10 value-matched | 已写入 `D15D_VERSION_MANIFEST.json`；VERSION_MAP 旧值不替代正式 G0 |
| Kaiming 终口径 | `OUT_OF_SCOPE / DEFERRED`（`PENDING_D/E_SIGN`） | 随 D15D 材料书面记录 |
| 旧 systemd README | 过时“尚无生产实现” | 属待修文档缺口；随 D15D PR 修，不改脚本 |

### 5.1 Phase 0 基线刷新（2026-09-10）

- `git merge-base --is-ancestor ba3b50e1bdeea185bca9daee9d1d45958f62a636 306c15ed8e1bb88378c849e83119828afd35083d`：PASS。
- `git diff --name-only ba3b50e..HEAD -- packaging/ memory-service/ cpp-bridge/ migrations/ config/`：无输出，判定 `DOCS_EVIDENCE_ONLY`。
- 当前守卫全套（D15A lock matrix + D14A packaging/provenance）为 `93 passed`；本卡原 37 项口径为历史子集，不再用于掩盖当前更大回归面。
- **Phase 1 blocker**：本地只发现 `kylin-memory-a-d14a-0.1.0-d14a-rebuilt-ba3b50e.tar.gz`，其 tar SHA 与 D14D 冻结身份 `2222c904...` 不一致；不得以此替代原始冻结包。需先取得原始包或获得人工批准重建并重跑全部一致性/证据链。
- **2026-09-10 C8 解除**：原始冻结 tar 已定位且哈希匹配；WSL `sha256sum -c SHA256SUMS` 的 3357 个普通文件 OK，3 个 `runtime/python/bin/python*` symlink 因 WSL 缺少 `/usr/bin/python3.12` 不可哈希。Runbook 已允许 `PASS_INHERITED_FROZEN_TAR_IDENTITY` 继承 D14D G2 对同一 frozen tar 的 integrity PASS；不得扩展到任何 regular-file mismatch。
- **2026-09-10 C10 返工**：既有 `Ducknesses` 授权 reference 已可审计，但该账号与
  PR #175 author 相同，仅作为 invalid historical authority record。按 Reviewer E
  第三轮治理澄清，本 D 轨是提出方案的一方，identity/provenance/contract consistency
  closure 的专项 authority 由 Reviewer E 独立裁定，无需另行 non-author ReviewerD
  独立会签。当前 identity 已同步到 D14D `ba3b50e` formal evidence，契约状态改为
  `PROPOSED v5 / REVIEWER_E_IDENTITY_ADJUDICATION_APPROVED / PENDING_FINAL_D15D_SIGN`
  （historical；后续 G-D7 signed 口径见第 7 节）。
  该专项裁定不改变 `release_ready=false`、`production_ready=false` 边界。
  随后 Reviewer E 在 PR comment
  [5616426125](https://github.com/Kylin-Agent-Competition/kylinOS-agent-memory/pull/175#issuecomment-5616426125)
  独立裁定 identity/provenance closure `APPROVED`；该裁定用于回填 contract、
  manifest 与 Round 2 evidence，不视为 ReviewerD 会签或 D15D 最终签署。
- **2026-09-10 C1 返工解除**：在 `E:\Kylin-memory-dev\tmp\d15d-c1-20260910T082403Z` 的 detached worktree 重建严格 C1：`HEAD=ba3b50e...`、完整 `git status --porcelain=v1 --untracked-files=all --ignored=no` 为 0 行、`ba3b50e...` 是 `kylin-mem/main@cdcce34...` 祖先。
- **2026-09-10 C2 第 4 轮失效**：Reviewer E 检出 current main 前移到 `4a6323f`。live
  diff `ba3b50e..4a6323f` 命中 7 个 `memory-service/evaluation|tests` 新增文件；
  其中 3 个 `evaluation/` 文件会真实装入 runtime/app，4 个 `tests/` 文件被 builder
  排除但仍触发整体 `memory-service/` 的 C2 policy。按 Runbook §4，
  本卡选择 **TRIGGER_NEW_RELEASE_PACKAGE_IDENTITY**：不自行剔除 `evaluation/`，不把旧
  frozen tar 继续声明为 current main 锁定包；必须从选定的新 release commit 干净重包、
  重算 hash 并重跑完整一致性与麒麟 VM 证据链。失效记录见
  `evidence/d15d-lock/20260910T105245Z/c2_main_drift_invalidation.md`。
- **2026-09-10 Round 6 新 release identity 已建立**：从 detached worktree
  `main@4a6323f` 干净重建 `4a6323f-r2` 包，三哈希回填 D15D manifest v2；C1、
  C3-C6、C8 与 clean tar smoke（install → migration → start → real SDK dim=768 →
  restart → rollback）已在麒麟 VM（127.0.0.1:2223）PASS，evidence root 为
  `evidence/d15d-lock/20260910T205300Z/`。首轮 pyc 污染与长驻服务干扰的失败
  run 原始日志保留；最终 smoke 的 `runtime/app` 为 0 个 `.pyc` / `__pycache__`。
  旧 D14D `ba3b50e` 证据只能标 `HISTORICAL_PASS_AT_ba3b50e /
  NOT_SUFFICIENT_FOR_NEW_RELEASE`，不得替代本轮新 release 证据。Reviewer E 已在
  Round 7 以 review `5168962906` 给出 `APPROVE / G-D7 SIGNED`；PR #175 的 merge
  commit 为 `ad782f5be747d9d59f273c1c0813535d62b50dcb`。
- **2026-09-10 Round 7 证据语义闭环**：Round 6 review 指出新 evidence root 尚缺少
  D14D-style root 元数据。现按 G-D4 备选路径正式声明：`20260910T205300Z` root
  承载当前 release binding，不承载完整 D14D G0–G9 L3 rerun；root 补齐
  `summary.json`、`gate_matrix.json`、`evidence_index.json`、`checksums.txt` 和
  `submission_inventory.json`，并机读 failed runs → final clean-tar PASS 的权威
  run 选择。`main@4a6323f..main@ad782f5` 的 raw packaging diff 只命中 PR #175
  已审查的 `packaging/systemd/README.md` docs-only exception，另新增两个 day15
  文档。C2 使用完整 locked-path scope 时，raw diff 命中同一 PR #175 已审查的
  `docs/day14/00_d14a_release_package_contract.md` 与 `packaging/systemd/README.md`
  两个 docs-only exception；后者是唯一 runtime-prefix hit。两个例外均已登记，
  C2 不触发 release/package identity 重建。

## 6. 失效与重签规则

以下任一变化都会使既有锁定失效：`release_commit` 变化、main 前进触及 `packaging/`、runtime/包 hash 变化、
`docs/day14/00` 契约升版、D14D 正式 L3 输入变化、D15D PR head 变化。失效后必须重建身份、重跑一致性核验、
更新版本清单并重新请 E 签署；旧记录保留为历史，不覆盖、不借旧 PASS。

## 7. 会签记录（G-D7 已签署）

- 任务卡起草：2026-09-06（本地 DRAFT）
- D 主审会签：NOT_REQUIRED_FOR_C10_IDENTITY_ADJUDICATION（Reviewer E 已澄清 D 轨
  为提出方案方；既有 `Ducknesses` 授权评论仅保留为 historical invalid record）
- Reviewer E 签署：`APPROVE / G-D7 SIGNED`（review `5168962906`，2026-09-10）
- 合并记录：PR #175 MERGED，merge commit `ad782f5be747d9d59f273c1c0813535d62b50dcb`
- D15D closeout 记录：PR #177 Reviewer E 对 `8dc21f7df7ea359b880efc93902569a5ca4187d8`
  APPROVE，squash merge commit `6e9f56d2983b36c3b174e7acf6687c4af79439d4`，
  merged at `2026-09-10T17:52:08Z`
