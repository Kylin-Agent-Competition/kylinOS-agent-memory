# D1-B Vector、Hybrid/RRF 与检索候选方案收口记录

- **日期**：2026-07-31
- **责任轨道**：B（Vector、FTS5、RRF 与检索评测）
- **作者侧状态**：`COMPLETE`
- **独立审核状态**：`PENDING`
- **合并状态**：`PENDING`
- **阶段边界**：本记录只关闭 D1-B 作者侧工作，不进入 D2 或 D3

## 1. 收口结论

D1-B 规定的三项工作均已有可审查交付物：

1. Vector Engine 服务、Collection、CRUD、过滤与持久化基线已在真实
   Kylin V11 虚拟机执行。首个数据面调用 `ShowCollections` 返回
   RPC `1002`，因此结论是 `BLOCKED / E3`，不是能力通过。
2. 原生 Hybrid/RRF 已完成源码与真实安装客户端 ABI 核定，最高证据为
   `ABI_VERIFIED / E3`。端到端结果保持 `UNTESTED`，未声明 E4 支持。
3. 应用层 `RetrievalCandidate` 与 RRF 默认方案草案已完成，明确了
   候选字段、RRF 公式、确定性排序、硬过滤、降级、解释和评测边界；
   所有需冻结的契约问题继续留给 D3。

因此，D1-B 的作者侧调查、证据记录和方案起草已经完成。Vector 数据面
阻塞是 D1 的真实结论和后续输入，不应通过提前执行 D2 任务来掩盖。

## 2. 任务—交付物映射

### D1-B-01 Vector Engine 基线

| 类型 | 路径 |
|---|---|
| C++ 探针 | `tests/vector-engine/d1_vector_baseline.cpp` |
| 运行脚本 | `scripts/run_d1_vector_baseline.sh` |
| 原始日志 | `evidence/l2-kylin-vm/d1-b-vector-engine-boundary-20260730.log` |
| 边界报告 | `evidence/l2-kylin-vm/d1-b-vector-engine-boundary-20260730.md` |
| 证据索引 | `evidence/index.yaml` 中 `D1-B-01` |

已确认：

- 服务状态、数据库占用、SDK 源码、客户端版本、编译与连接检查已完成；
- `ShowCollections` 返回 RPC `1002`；
- Collection、CRUD、过滤、向量检索和重启持久化没有执行；
- 原数据库、服务和系统包未被测试清理或候选包覆盖；
- 解除阻塞条件已写入报告。

### D1-B-02 原生 Hybrid/RRF 证据等级

| 类型 | 路径 |
|---|---|
| 原始日志 | `evidence/l2-kylin-vm/d1-b-native-hybrid-rrf-evidence-20260731.log` |
| 核定报告 | `evidence/l2-kylin-vm/d1-b-native-hybrid-rrf-evidence-20260731.md` |
| 证据索引 | `evidence/index.yaml` 中 `D1-B-02` |

已确认：

- 官方 SDK 存在 `HybridSearch`、`RRFRanker` 和 `WeightedRanker` 路径；
- Kylin 宿主安装库导出对应 ABI；
- 原生 Hybrid/RRF 没有成功运行证据；
- 默认产品路径仍是应用层 RRF。

### D1-B-03 检索候选与应用层 RRF 草案

| 类型 | 路径 |
|---|---|
| 方案草案 | `docs/architecture/d1-b-retrieval-candidate-rrf-draft.md` |
| 架构索引 | `docs/architecture/README.md` |

草案已覆盖：

- `RetrievalHit` 与 `RetrievalCandidate` 两层候选模型；
- 1 起始排名、候选 `k=60`、双路等权的应用层 RRF；
- 用户、状态、有效期、敏感、遗忘和冲突硬过滤；
- FTS5-only、Vector-only、双路失败和 deadline 降级；
- 可解释字段、确定性 tie-break 和后续评测要求；
- D3 待冻结问题，未提前写成正式契约。

## 3. D1 边界审查

本分支相对 D1 起点新增的是测试探针、运行脚本、证据、调查报告和架构
草案，没有实现 D2/D3/D4 生产能力。

明确未执行：

- 未把 RPC `1002` 记为 Vector CRUD 或持久化成功；
- 未运行 D2 的真实 Insert/Search/Filter/Delete 与重启持久化实验；
- 未创建正式 `VectorProvider`、`IndexState` 或生产
  `RetrievalCandidate`；
- 未冻结 RRF 参数、枚举、IPC 字段或错误契约；
- 未进入 FTS5/Vector 统一候选实现；
- 未推送、创建 PR 或合并当前功能分支。

## 4. 验证记录

- 当前分支已 rebase 到 `origin/main@bc51c37`；
- 三方合并复检无冲突；
- `evidence/index.yaml` 使用统一 Schema `1.1`；
- A/B 两轨共 5 条索引记录的主要证据文件均存在且 SHA-256 匹配；
- 仓库基线结构检查通过，错误数为 0；
- `check_kylin_environment.sh`、`run_d1_vector_baseline.sh` 和
  `verify_repository_baseline.sh` 语法检查通过；
- Git 空白检查通过；
- 工作树在收口改动前保持干净。

## 5. 未关闭的外部事项

以下事项不应伪装成作者侧已完成，也不授权继续到 D2：

1. D 主 Reviewer 对 D1-B-01、D1-B-02 和 D1-B-03 的独立审查；
2. 对证据结论、用户隔离和指标影响的必要补审；
3. 当前分支的推送、PR、合并与台账勾选；
4. 环境管理员提供协议兼容、配置与可信模块依赖完整的 Vector Engine；
5. 阻塞解除后由新指令启动 D2-B 真实数据面实验。

## 6. 停止条件

本记录提交并通过本地验证后，D1-B 作者侧内容视为完成。本分支在此停止：

- 不自动进入 D2；
- 不提前执行 D3 契约冻结；
- 不以本记录替代独立 Reviewer 结论；
- 等待人工审核、推送/PR 指令或明确的下一阶段授权。
