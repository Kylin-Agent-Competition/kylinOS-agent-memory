# systemd 服务

本目录存放 Memory Service 的 systemd service unit 文件和安装/卸载脚本。

## 当前状态

**历史/只读。** 正式发布入口为 `packaging/release/`；本目录保留既有 unit 与
历史脚本，不作为新部署入口。发布包安装、验证和回退语义以
`docs/day14/00_d14a_release_package_contract.md` 为准。
