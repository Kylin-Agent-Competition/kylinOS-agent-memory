# D2-C 阶段性宿主测试报告（2026-08-11）

## 结论

本轮仅完成了临时麒麟虚拟机的模型可用性验证与采集前置检查，**未形成 D2-C Gate 可采信的正式证据**。PR #19 继续保持 `BLOCKED`，不得据此合并或将 AGT-004 / AGT-005 标记为 `HOST_VERIFIED`。

## 已验证结果

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| AI 助手真实文本响应 | 已验证 | 完成本地/云端模型配置后，助手对最小输入生成中文回复。该结果仅证明当前临时机具备推理能力。 |
| DeepSeek API 基础 HTTPS 连通性 | 已验证 | 无认证 `HEAD https://api.deepseek.com` 返回 HTTP 401，表明 DNS、TLS 与路由可达；401 符合未携带认证的预期。 |
| D2-C 采集脚本部署 | 阻塞 | 临时机不存在 `~/d2c-probe`；从 GitHub 默认分支拉取脚本时，GitHub 443 连接超时。 |
| 默认分支采集基线 | 阻塞 | `origin/main` 不含 `scripts/d2c_evidence_collector.sh`。采集脚本只存在于本 PR 未合并变更，按协作规则不能作为正式宿主测试共同基线。 |

## 本轮环境处置

- 仅操作一次性虚拟机 `D2C-Retest-runtime-baseline-20260809`；未改动原始模板。
- 修复并重启了临时机中的数据管理服务；安装 `kylin-bot` 仅用于排查模型管理能力。
- `kylin-bot` 被确认是独立 Provider CLI，不能替代麒麟 AI 助手所需的本地模型登记入口。
- 未向仓库提交 API Key、模型配置、原始截图、原始日志或虚拟机状态。

## 未完成的 D2-C 准入项

1. 以已合并、可复现的 D2-C 采集工具为基线部署到麒麟 VM。
2. PostTurn：DB 快照、15 秒稳定性、UI/RECORD 一致性。
3. PreChat：UI 截图、RECORD 消息、可解码的实际模型请求（含/不含 Memory Context 的判定）。
4. Tool：真实成功、失败、取消三类 `ToolExecutionEvent`。
5. 按 `evidence/README.md` 更新证据索引、受控存储链接与 SHA-256，并完成 D/E 复审。

## 下一步

先将 D2-C 采集工具合并到默认分支，或由 D/E 明确批准本次临时测试所用的具体提交为共同基线；随后在网络可达的麒麟 VM 重新部署并完整重跑三项实验。
