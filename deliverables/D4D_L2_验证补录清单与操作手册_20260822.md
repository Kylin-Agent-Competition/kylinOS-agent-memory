# D4D 麒麟 VM L2 验证 —— 补录数据清单与操作手册

- **日期**：2026-08-22
- **依据**：`evidence/l2-kylin-vm/d4d_vm_verify_20260821/verify_run.log` vs `RESULT.md` 存在证据矛盾
- **待补录范围**：FTS5/busy_timeout/UDS 三脚本重跑 + systemd socket 权限位 + 归档一致性
- **涉及代码缺陷**：`db/engine.py::init_schema()` 触发器创建非幂等（`CREATE TRIGGER` 无条件执行，二次调用崩溃）

---

## 一、补录数据清单

| # | 待补录项 | 缺失/异常原因 | 补录动作 | 优先级 | 验收标准 |
|---|---------|--------------|---------|:---:|---------|
| 1 | **FTS5 中文检索 + 软删除同步：PASS** | `verify_run.log` 归档段脚本崩溃：`sqlite3.OperationalError: trigger memory_fts_ai already exists`（`init_schema()` 二次调用非幂等） | 修复 `init_schema()` → 清理临时库 → 重跑 `vfy_fts5.py` | **P0** | 末尾输出 `FTS5 中文检索 + 软删除同步: PASS`，4 行 MATCH 断言无 AssertionError |
| 2 | **busy_timeout 持锁降级：PASS** | 同上（`vfy_busy.py` 在 `init_schema()` 处崩溃） | 修复后重跑 `vfy_busy.py` | **P0** | 末尾 `busy_timeout 持锁降级语义: PASS`，异常识别为锁定降级，耗时 1.5~6s |
| 3 | **UDS 断开/超时/停止语义：PASS** | 同上（`vfy_uds.py` 在 `init_schema()` 处崩溃） | 修复后重跑 `vfy_uds.py` | **P0** | 6.1~6.7 全 PASS，末尾 `UDS 断开/超时/停止语义: PASS` |
| 4 | **systemd socket 权限位证据（0600 `srw-------`）** | 归档日志 `verify_run.log` 仅含 `status/journal`，未含 socket 文件 `ls -la` / `stat` 输出 | 服务启动时补录 `ls -la` + `stat -c` | **P1** | 输出含 `srw-------` + 权限八进制 `600`（或 0600）+ 属主 `kylin-agent:kylin-agent` |
| 5 | **归档一致性（verify_run.log ↔ RESULT.md）** | `RESULT.md` 全部标 PASS，但 `verify_run.log` 归档段三脚本实际报错 | 修复后整体重跑归档，两文件结果一致 | **P0** | `RESULT.md` FTS5/busy/UDS 三栏 = `PASS`，且 `verify_run.log` 中对应脚本输出无 Traceback |
| 6 | **Alembic 正确调用方式实录** | 手册第 2 步 `cd migrations && alembic` 报 `Path doesn't exist: migrations`（已在 RESULT.md 注记，但无成功实录） | 补录 `alembic -c migrations/alembic.ini upgrade head` 成功输出 | P2 | `verify_run.log` 含 `alembic -c migrations/alembic.ini` 且 `EXIT=0`、schema 完整 |
| 7 | **UDS stop 未 unlink 技术债登记** | `UDSGatewayServer.stop()` 不删除 socket 文件，Linux 停后 connect 成功、请求被 ECONNRESET 拒绝 | 在技术债文档登记 **TD-IPC**（可选：同步修复 `stop()` 中 unlink） | P2 | 技术债登记含：现象、复现、影响、建议修复点 `server.py:stop()` |

---

## 二、前置准备

### 2.1 环境
- 麒麟 VM：银河麒麟桌面 V11（x86_64），用户 `kylin-agent`，SSH `127.0.0.1:2222`
- VM 仓库：`~/kylinOS-agent-memory`（分支 `feat/d4d-ipc-db-outbox` @ `ed9949c`）
- VM venv：`~/d4d-venv/bin/python`（已验证 sqlalchemy 2.0.52 / alembic 1.19.1）
- VM 验证目录：`/tmp/kylin-memory-vfy`

### 2.2 宿主侧 SSH 凭据
- 密码来自环境变量 `KYLIN_VM_PASSWORD`（沿用现有 `scripts/*.py` 约定，禁止硬编码）

---

## 三、代码修复（Step A）—— 触发器幂等

> 根因：`db/engine.py::init_schema()` 在已存在 `memory_fts` 表时**只检查表、不检查触发器**，
> 对 `FTS_TRIGGERS_DDL` 四条 `CREATE TRIGGER` 无条件执行 → 二次调用即 `trigger already exists`。
> 最小修复：在 DDL 上加 `IF NOT EXISTS`（SQLite 原生支持，不改变冻结语义）。

### A.1 修改 `memory-service/db/schema.py`

将 `FTS_TRIGGERS_DDL` 中 4 条 `CREATE TRIGGER` 改为 `CREATE TRIGGER IF NOT EXISTS`：

```diff
     # INSERT：新记录入 FTS
     """
-    CREATE TRIGGER memory_fts_ai AFTER INSERT ON memory_entries BEGIN
+    CREATE TRIGGER IF NOT EXISTS memory_fts_ai AFTER INSERT ON memory_entries BEGIN
         INSERT INTO memory_fts(rowid, content, entry_type, user_id)
         VALUES (new.id, new.content, new.entry_type, new.user_id);
     END
     """,
     # UPDATE（内容变化，非软删除）：先删旧再插新
     """
-    CREATE TRIGGER memory_fts_au_content AFTER UPDATE ON memory_entries
+    CREATE TRIGGER IF NOT EXISTS memory_fts_au_content AFTER UPDATE ON memory_entries
     WHEN old.is_deleted = 0 AND new.is_deleted = 0 AND old.content IS NOT new.content
     BEGIN
         DELETE FROM memory_fts WHERE rowid = old.id;
         INSERT INTO memory_fts(rowid, content, entry_type, user_id)
         VALUES (new.id, new.content, new.entry_type, new.user_id);
     END
     """,
     # UPDATE（软删除 0→1）：只删 FTS，不插回（MATCH 不再命中，冻结文档 §2.4）
     """
-    CREATE TRIGGER memory_fts_au_deleted AFTER UPDATE ON memory_entries
+    CREATE TRIGGER IF NOT EXISTS memory_fts_au_deleted AFTER UPDATE ON memory_entries
     WHEN old.is_deleted = 0 AND new.is_deleted = 1
     BEGIN
         DELETE FROM memory_fts WHERE rowid = old.id;
     END
     """,
     # DELETE：物理删除同步删 FTS
     """
-    CREATE TRIGGER memory_fts_ad AFTER DELETE ON memory_entries BEGIN
+    CREATE TRIGGER IF NOT EXISTS memory_fts_ad AFTER DELETE ON memory_entries BEGIN
         DELETE FROM memory_fts WHERE rowid = old.id;
     END
     """,
```

### A.2 同步修改 `migrations/versions/001_initial_schema.py`

迁移只执行一次，但为与 `schema.py` 单一真相一致，同步改为 `IF NOT EXISTS`（alembic 按 revision id 记录，不校验内容，修改安全）：

```diff
-    op.execute(
-        """
-        CREATE TRIGGER memory_fts_ai AFTER INSERT ON memory_entries BEGIN
+    op.execute(
+        """
+        CREATE TRIGGER IF NOT EXISTS memory_fts_ai AFTER INSERT ON memory_entries BEGIN
             INSERT INTO memory_fts(rowid, content, entry_type, user_id)
             VALUES (new.id, new.content, new.entry_type, new.user_id);
         END
         """
     )
```

（`memory_fts_au_content` / `memory_fts_au_deleted` / `memory_fts_ad` 三条同样加 `IF NOT EXISTS`。）

### A.3 本地快速自检

```bash
cd memory-service
python -c "
import db.engine as e
from sqlalchemy import create_engine
eng = create_engine('sqlite:////tmp/idem_check.db')
e.init_schema(eng)   # 第一次
e.init_schema(eng)   # 第二次：修复前崩溃，修复后应无异常
print('IDEMPOTENT_OK')
"
```

---

## 四、同步代码到 VM（Step B）

### 方式 1：git 更新（推荐，保留 commit 证据）

在 VM 终端执行：

```bash
cd ~/kylinOS-agent-memory
git checkout feat/d4d-ipc-db-outbox
git pull   # 或宿主 push 修复后 pull
git log --oneline -1   # 记下新 commit
```

### 方式 2：宿主 SSH 直接覆盖（未 push 时）

宿主 PowerShell（`$env:KYLIN_VM_PASSWORD` 已设置）：

```powershell
# 用 scp 覆盖两个文件（需先获取 sftp 或 scp 支持）
scp -P 2222 memory-service/db/schema.py kylin-agent@127.0.0.1:~/kylinOS-agent-memory/memory-service/db/schema.py
scp -P 2222 migrations/versions/001_initial_schema.py kylin-agent@127.0.0.1:~/kylinOS-agent-memory/migrations/versions/001_initial_schema.py
```

> 注意：两个文件都需要写入后，在 VM 侧执行一次 sha256 校验：
> `sha256sum ~/kylinOS-agent-memory/memory-service/db/schema.py` 与本地一致。

---

## 五、清理 + 重跑验证脚本（Step C）

> 关键：**必须先删除 `/tmp/kylin-memory-vfy` 中的旧临时库与 socket**，
> 否则 `init_schema()` 在旧库上仍会因旧触发器报错（修复只保证"新库重复调用"幂等，
> 旧库若已含触发器则首次 CREATE 即冲突）。

在 VM 终端执行：

```bash
# C.1 清理旧临时库 / socket
rm -f /tmp/kylin-memory-vfy/*.db /tmp/kylin-memory-vfy/*.sock

# C.2 重跑第 4 步：FTS5 中文检索 + 软删除
~/d4d-venv/bin/python /tmp/kylin-memory-vfy/vfy_fts5.py
# 预期：MATCH '咖啡' -> 1 / MATCH '我喜欢喝咖啡' -> 1
#      软删除后两者 -> 0 / 主表保留 2
#      FTS5 中文检索 + 软删除同步: PASS

# C.3 重跑第 5 步：busy_timeout 降级
~/d4d-venv/bin/python /tmp/kylin-memory-vfy/vfy_busy.py
# 预期：异常类型: OperationalError / 识别为锁定降级: True / 耗时约 2.x s
#      busy_timeout 持锁降级语义: PASS

# C.4 重跑第 6 步：UDS 断开/超时/停止
~/d4d-venv/bin/python /tmp/kylin-memory-vfy/vfy_uds.py
# 预期：6.1~6.7 全 PASS（6.7 按 Linux 语义：连接被重置/无响应）→ UDS 断开/超时/停止语义: PASS
```

> 若 `/tmp/kylin-memory-vfy` 下无验证脚本（VM 已清理），先从宿主把 `scripts/l2_vm_run_tests.py`
> 内嵌的三段脚本（`VFY_FTS5` / `VFY_BUSY` / `VFY_UDS`）写回 VM，或直接重跑：
> `python scripts/l2_vm_run_tests.py`（该脚本已含修复版 6.7 判定 + 清理逻辑）。

---

## 六、补录 systemd socket 权限位（Step D）

在 VM 终端执行（服务需处于 running 状态）：

```bash
# D.1 确保服务运行
export XDG_RUNTIME_DIR=/run/user/1000
systemctl --user start kylin-memory
sleep 2

# D.2 权限位（验收：srw------- + 0600 + kylin-agent:kylin-agent）
ls -la $XDG_RUNTIME_DIR/kylin-memory/memory.sock
stat -c '权限八进制=%a 权限位=%A 属主=%U:%G' $XDG_RUNTIME_DIR/kylin-memory/memory.sock

# D.3 留档（追加到归档日志）
echo "=== socket 权限位 ===" >> ~/kylinOS-agent-memory/evidence/l2-kylin-vm/d4d_vm_verify_20260821/verify_run.log
stat -c '%a %A %U:%G %n' $XDG_RUNTIME_DIR/kylin-memory/memory.sock >> ~/kylinOS-agent-memory/evidence/l2-kylin-vm/d4d_vm_verify_20260821/verify_run.log
```

---

## 七、重新归档（Step E）—— 保证 RESULT.md ↔ verify_run.log 一致

### E.1 完整重跑归档（推荐，复用 run_tests 脚本）

宿主 PowerShell（需 `$env:KYLIN_VM_PASSWORD`）：

```powershell
python scripts\l2_vm_run_tests.py
```

该脚本会按"修复版"流程执行第 2~7 步，重新生成
`evidence/l2-kylin-vm/d4d_vm_verify_20260821/verify_run.log` + `RESULT.md`
并下载回本地，消除此前矛盾。

### E.2 若仅需补录 FTS5/busy/UDS + disk 权限（不重跑全量）

VM 终端手工重写 `RESULT.md`（按实际结果填 PASS/FAIL）：

```bash
cat > ~/kylinOS-agent-memory/evidence/l2-kylin-vm/d4d_vm_verify_20260821/RESULT.md <<'EOF'
# D4D VM L2 验证结果（2026-08-21 补录 2026-08-22）
- commit: <新commit>
- 迁移验收（2.1 upgrade / 2.2 schema / 2.3 往返）: PASS / PASS / PASS
- systemd 部署（启动/重启/回退/日志/socket）: PASS / PASS / PASS / PASS / PASS
- FTS5 中文+软删除: PASS
- busy_timeout 降级: PASS
- UDS 断开/超时: PASS
- socket 权限位: srw------- / 0600 / kylin-agent:kylin-agent
- 执行人: kylin-agent（手动执行，SSH 自动化辅助）
- 补录说明: 2026-08-22 修复 init_schema() 触发器幂等后重跑第 4/5/6 步；归档重跑不再报 trigger already exists
- 缺陷注记1: alembic.ini script_location=migrations，须在仓库根以 -c migrations/alembic.ini 执行
- 缺陷注记2: UDSGatewayServer.stop() 未 unlink socket 文件；Linux 下停后 connect 成功、请求被 ECONNRESET 拒绝（建议登记技术债 TD-IPC）
EOF
```

### E.3 归档内容自校验（Step F）

```bash
cd ~/kylinOS-agent-memory/evidence/l2-kylin-vm/d4d_vm_verify_20260821
# 校验 1：verify_run.log 中三脚本无 Traceback
grep -c "Traceback" verify_run.log || echo "NO_TRACEBACK(通过)"
# 校验 2：verify_run.log 三脚本 PASS 标识
grep -E "FTS5 中文检索 \+ 软删除同步: PASS|busy_timeout 持锁降级语义: PASS|UDS 断开/超时/停止语义: PASS" verify_run.log
# 校验 3：RESULT.md 与 verify_run.log 一致（人工核对 FTS5/busy/UDS 三栏）
cat RESULT.md
```

---

## 八、验收清单（全部满足即补录完成）

- [x] **A**：`schema.py` 与 `001_initial_schema.py` 触发器 DDL 已加 `IF NOT EXISTS`，本地幂等自检 `IDEMPOTENT_OK`（证据：`verify_run.log` L8-9；回归 `test_db_d4d.py::test_init_schema_idempotent`）
- [x] **B**：修复后代码已同步 VM 并经 sha256 校验（`scripts/l2_vm_supplement_20260822.py` step1 逐文件 sha256 `MATCH`）
- [x] **C**：`vfy_fts5.py` / `vfy_busy.py` / `vfy_uds.py` 三脚本重跑全 PASS，无 Traceback（证据：`verify_run.log` L129-149，三 PASS）
- [x] **D**：socket 权限位补录 `srw-------` + `0600` + `kylin-agent:kylin-agent`（证据：`verify_run.log` L127-128）
- [x] **E**：`verify_run.log` 与 `RESULT.md` 结果一致（FTS5/busy/UDS 三栏 = PASS；`RESULT.md` L5-7）
- [x] **F**：归档自校验：`NO_TRACEBACK`，三 PASS 标识齐全（全文件无 `Traceback`，`SOCK_UNLINKED_OK` 见 `verify_run.log` L151）
- [x] **备注**：TD-IPC（stop 未 unlink）已登记且已修复（`TECHNICAL_DEBT_REGISTER.md` L63 `TD-IPC-001` Resolved；`gateway/server.py::stop()` 增加 unlink；回归 `test_gateway_server_d4d.py::test_stop_unlinks_socket`）

> 2026-08-22 已全部落实，证据已归档至 `evidence/l2-kylin-vm/d4d_vm_verify_20260821/{verify_run.log, RESULT.md}`。剩余收尾：将上述代码/证据/登记变更提交并推送（见 Step B 方式 1）。

---

## 九、技术债与后续建议（与本补录同源）

| 技术债 | 现象 | 建议 |
|--------|------|------|
| TD-IPC（登记中） | `UDSGatewayServer.stop()` 未 unlink socket；Linux 停后 connect 成功、请求 ECONNRESET | 在 `stop()` 中 `close` 后 `os.unlink(self._socket_path)`，加 try/except 容错；并补回归用例 |
| init_schema 幂等（本次已修） | 二次调用建库触发器冲突 | 已通过 `CREATE TRIGGER IF NOT EXISTS` 修复；建议在 `engine.py` 注释中标注幂等契约 |
| alembic 调用路径（已注记） | 手册 `cd migrations` 失败 | 手册/脚本统一改用 `alembic -c migrations/alembic.ini`（run_tests.py 已实现） |

---

## 附：供宿主侧一键执行的补录脚本参考

> 复用现有 `scripts/l2_vm_run_tests.py` 即可覆盖大部分补录；若只想补录三脚本 + socket 权限，
> 可基于 `scripts/l2_vm_diag_systemd.py` / `scripts/l2_vm_diag_step2_6.py` 的 SSH 框架扩展，
> 密码统一从 `KYLIN_VM_PASSWORD` 读取，禁止硬编码。